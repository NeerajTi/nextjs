/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    basePath: '',
    trailingSlash: true,
    images: {
      unoptimized: true
  }
  }
  
  module.exports = nextConfig